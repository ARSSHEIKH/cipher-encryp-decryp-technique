import React from 'react'


const ArrowFirstCompo = () => <div>Hello Arrow First Components</div>


// function ArrowFirstCompo() {
//         var pt_str = (prompt("Enter plain text"));
//         var key_str = prompt("Enter key");

//         if(pt_str.length == key_str.length){
//             var pt_ch = [pt_str.length];
//             // pt_ch = Array.from(pt_str.toString().toUpperCase());
//             // console.log(pt_ch)

//             var key_ch = [key_str.length];
//             // key_ch = Array.from(key_str.toString().toUpperCase());
//             // console.log(key_ch)
            
//             for(let i=0; i<=pt_str.length-1; i++){
//                 pt_ch[i] = pt_str.charCodeAt(i);
//             }
//             for(let i=0; i<=key_str.length-1; i++){
//                 key_ch[i] = key_str.charCodeAt(i);
//             }
//             console.log(key_ch);
//         }
        
//     return (
//         <div>key_ch[0]</div>
        
//     );
// }

export default ArrowFirstCompo